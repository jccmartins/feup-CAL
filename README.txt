Dependencies:
    cmake

Build cmake files:
    Inside src folder, create a new folder
    Open a terminal inside that new folder and run 'cmake -H.. .'
    
Compile and Run:
    In the same terminal run 'make clean && make' to compile the program, and then './project' to run it
